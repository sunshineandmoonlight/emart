# Emart 电商平台 - 本地运行指南

## 目录
1. [环境准备](#环境准备)
2. [数据库和Redis启动](#数据库和redis启动)
3. [后端启动](#后端启动)
4. [前端启动](#前端启动)
5. [访问地址](#访问地址)
6. [常见问题](#常见问题)

---

## 环境准备

### 必需软件

1. **JDK 17**
   - 下载地址：https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html
   - 或使用 OpenJDK 17：https://adoptium.net/
   - 验证安装：`java -version`（应显示 17.x.x）

2. **Maven 3.8+**
   - 下载地址：https://maven.apache.org/download.cgi
   - 或使用项目自带的 Maven Wrapper（./mvnw）
   - 验证安装：`mvn -v`

3. **Node.js 18+**
   - 下载地址：https://nodejs.org/
   - 验证安装：`node -v` 和 `npm -v`
   - **推荐**：使用 nvm 管理多个 Node 版本

4. **MySQL 8.0**
   - 下载地址：https://dev.mysql.com/downloads/mysql/
   - 或使用 Docker（推荐，见下方）

5. **Redis 7.x**
   - 下载地址：https://redis.io/download
   - Windows用户：https://github.com/tporadowski/redis/releases
   - 或使用 Docker（推荐，见下方）

6. **Git**（可选）
   - 下载地址：https://git-scm.com/

### 可选软件（推荐）

- **IDE**：IntelliJ IDEA / VS Code
- **API测试**：Postman / Apifox
- **数据库管理**：Navicat / DBeaver / MySQL Workbench
- **Redis管理**：RedisInsight / Another Redis Desktop Manager

---

## 数据库和Redis启动

### 方式一：使用Docker（推荐）✅

**前提**：已安装 Docker Desktop

**步骤**：

1. **启动MySQL和Redis**
   ```bash
   # 在项目根目录执行
   docker-compose up -d mysql redis

   # 查看容器状态
   docker-compose ps

   # 应该看到以下容器运行：
   # emart-mysql    Up    0.0.0.0:3306->3306/tcp
   # emart-redis    Up    0.0.0.0:6379->6379/tcp
   ```

2. **导入数据库**
   ```bash
   # 方式1：使用docker exec导入（推荐）
   docker exec -i emart-mysql mysql -uroot -pC20041109 emart < emart_backup.sql

   # 方式2：进入MySQL容器手动导入
   docker exec -it emart-mysql mysql -uroot -pC20041109
   mysql> use emart;
   mysql> source /docker-entrypoint-initdb.d/emart_backup.sql;

   # 方式3：使用Navicat等工具导入
   # 连接信息：localhost:3306, root, C20041109
   ```

3. **验证数据库**
   ```bash
   # 进入MySQL容器
   docker exec -it emart-mysql mysql -uroot -pC20041109

   # 查看数据库
   mysql> show databases;
   # 应该看到：emart

   # 查看表
   mysql> use emart;
   mysql> show tables;
   # 应该看到：ums_user, pms_product, oms_cart, oms_order 等

   # 退出
   mysql> exit;
   ```

4. **验证Redis**
   ```bash
   # 进入Redis容器
   docker exec -it emart-redis redis-cli

   # 测试连接
   > ping
   # 应该返回：PONG

   # 退出
   > exit
   ```

---

### 方式二：本地安装MySQL和Redis

#### 1. 安装和配置MySQL 8.0

**Windows**：
```bash
# 1. 下载MySQL Installer并安装
# 2. 启动MySQL服务
net start MySQL80

# 3. 登录MySQL
mysql -uroot -p

# 4. 创建数据库和用户
CREATE DATABASE emart CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'emart'@'localhost' IDENTIFIED BY 'C20041109';
GRANT ALL PRIVILEGES ON emart.* TO 'emart'@'localhost';
FLUSH PRIVILEGES;

# 5. 导入数据
USE emart;
SOURCE D:/test/project/emart/emart_backup.sql;
```

**Linux/Mac**：
```bash
# 1. 安装MySQL
# Ubuntu/Debian:
sudo apt-get install mysql-server

# Mac:
brew install mysql

# 2. 启动MySQL服务
sudo systemctl start mysql

# 3. 导入数据（同Windows步骤4-5）
```

#### 2. 安装和配置Redis

**Windows**：
```bash
# 1. 下载Redis for Windows
# 2. 解压后运行
redis-server.exe

# 3. 测试连接（新开终端）
redis-cli
> ping
# 应该返回：PONG
```

**Linux/Mac**：
```bash
# 1. 安装Redis
# Ubuntu/Debian:
sudo apt-get install redis-server

# Mac:
brew install redis

# 2. 启动Redis
sudo systemctl start redis  # Linux
brew services start redis    # Mac

# 3. 测试连接
redis-cli
> ping
```

---

## 后端启动

### 步骤1：修改配置文件

**检查并修改配置文件**：`src/main/resources/application-dev.yml`

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/emart?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root          # 如果使用Docker，保持root
    password: C20041109     # 你的MySQL密码
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost
    port: 6379
    password: 123456        # 你的Redis密码（如果没设置密码，删除此行）
    database: 0

# 邮件配置（可选，如果不发送邮件可以不配置）
mail:
  host: smtp.qq.com
  port: 587
  username: your-email@qq.com        # 你的QQ邮箱
  password: your-mail-auth-code      # QQ邮箱授权码（不是登录密码！）
```

**获取QQ邮箱授权码**：
1. 登录QQ邮箱 → 设置 → 账户
2. 开启POP3/SMTP服务
3. 生成授权码（16位字符）

### 步骤2：编译项目

```bash
# 在项目根目录执行
./mvnw clean install

# Windows用户使用：
mvnw.cmd clean install

# 或者跳过测试（推荐首次运行）
./mvnw clean install -DskipTests
```

**可能遇到的问题**：

**问题1**：Maven下载依赖慢
```bash
# 解决方案：配置国内镜像（阿里云）
# 编辑 ~/.m2/settings.xml 或 项目根目录/.mvn/settings.xml
# 添加以下内容：
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <name>Aliyun Maven</name>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

**问题2**：编译失败
```bash
# 清理并重新编译
./mvnw clean
./mvnw compile
```

### 步骤3：启动后端

**方式1：使用Maven启动**
```bash
./mvnw spring-boot:run

# Windows用户：
mvnw.cmd spring-boot:run
```

**方式2：使用IDE启动（推荐）**
1. 使用IntelliJ IDEA打开项目
2. 等待Maven依赖下载完成
3. 找到 `EmartApplication.java`（src/main/java/com/emart/EmartApplication.java）
4. 右键 → Run 'EmartApplication'

**方式3：打包后运行**
```bash
# 1. 打包
./mvnw clean package -DskipTests

# 2. 运行JAR文件
java -jar target/emart-api-1.0.0.jar
```

### 步骤4：验证后端启动

**查看控制台日志**，应该看到：
```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/
 :: Spring Boot ::               (v2.7.18)

2025-01-01 10:00:00.000  INFO --- [main] c.e.EmartApplication : Started EmartApplication in 15.234 seconds
```

**测试API**：
```bash
# 方式1：浏览器访问
http://localhost:8080/actuator/health
# 应该返回：{"status":"UP"}

# 方式2：使用curl
curl http://localhost:8080/actuator/health

# 方式3：访问Swagger文档
http://localhost:8080/swagger-ui.html
```

**如果启动失败**，查看常见问题部分。

---

## 前端启动

项目有两个前端：
- **emart-web**：用户端（购物、下单）
- **emart-admin**：管理后台（统计、管理）

### 步骤1：安装依赖

**安装emart-web依赖**：
```bash
cd emart-web

# 如果之前没有安装过依赖
npm install

# 或者使用国内镜像（推荐）
npm install --registry=https://registry.npmmirror.com
```

**安装emart-admin依赖**：
```bash
cd emart-admin

npm install --registry=https://registry.npmmirror.com
```

**常见问题**：
```
问题：npm install 速度慢或失败
解决：使用淘宝镜像
npm config set registry https://registry.npmmirror.com
```

### 步骤2：配置API地址

**检查emart-web的API配置**：
```bash
# 查看emart-web/src/utils/request.js 或类似文件
# 确认baseURL是否指向后端地址
```

通常前端会自动调用 `http://localhost:8080`，如果有跨域问题，需要配置代理。

**检查Vite配置**：`emart-web/vite.config.js`
```javascript
export default defineConfig({
  server: {
    port: 5173,        // 前端端口
    proxy: {
      '/api': {
        target: 'http://localhost:8080',  // 后端地址
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      }
    }
  }
})
```

### 步骤3：启动前端（用户端）

```bash
# 在emart-web目录执行
npm run dev

# 应该看到：
# VITE v7.2.4  ready in xxx ms
# ➜  Local:   http://localhost:5173/
# ➜  Network: use --host to expose
```

**访问用户端**：http://localhost:5173

### 步骤4：启动前端（管理端）

**新开一个终端窗口**：
```bash
cd emart-admin
npm run dev

# 应该看到：
# ➜  Local:   http://localhost:5174/
```

**访问管理后台**：http://localhost:5174

---

## 访问地址

启动成功后，可以通过以下地址访问：

### 后端
- **API文档**：http://localhost:8080/swagger-ui.html
- **健康检查**：http://localhost:8080/actuator/health
- **API根路径**：http://localhost:8080

### 前端
- **用户端**：http://localhost:5173
- **管理后台**：http://localhost:5174

### 数据库管理
- **MySQL**：localhost:3306（root / C20041109）
- **Redis**：localhost:6379（如果设置了密码：123456）

### 测试账号

**用户账号**（需要自行注册）：
```
用户名：testuser
密码：123456
```

**管理员账号**（数据库初始化脚本中创建）：
```sql
-- 查看管理员账号
SELECT * FROM ums_admin WHERE username = 'admin';
-- 默认密码可能需要重置
```

---

## 常见问题

### 问题1：后端启动失败 - 连接MySQL失败

**错误信息**：
```
CommunicationsException: Communications link failure
```

**解决方案**：
1. 确认MySQL已启动：`docker-compose ps` 或 `net start MySQL80`
2. 检查配置文件中的MySQL连接信息
3. 测试MySQL连接：
   ```bash
   mysql -hlocalhost -uroot -pC20041109
   ```
4. 检查MySQL端口是否被占用：
   ```bash
   # Windows
   netstat -ano | findstr :3306

   # Linux/Mac
   lsof -i :3306
   ```

---

### 问题2：后端启动失败 - 连接Redis失败

**错误信息**：
```
Unable to connect to Redis
```

**解决方案**：
1. 确认Redis已启动
   ```bash
   # Docker方式
   docker-compose ps

   # 本地方式
   redis-cli ping
   ```
2. 测试Redis连接：
   ```bash
   redis-cli -h localhost -p 6379
   > ping
   ```
3. 如果Redis设置了密码，确认配置文件中密码正确

---

### 问题3：前端无法调用后端API（跨域问题）

**错误信息**：
```
Access to XMLHttpRequest at 'http://localhost:8080/...' from origin 'http://localhost:5173' has been blocked by CORS policy
```

**解决方案**：

**方式1：后端配置CORS（推荐）**

检查后端CORS配置：`src/main/java/com/emart/config/CorsConfig.java`

```java
@Configuration
public class CorsConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                .allowedOriginPatterns("*")
                .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
                .allowedHeaders("*")
                .allowCredentials(true)
                .maxAge(3600);
    }
}
```

**方式2：前端使用代理**

在 `vite.config.js` 中配置代理：
```javascript
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      }
    }
  }
})
```

---

### 问题4：Maven依赖下载失败

**解决方案**：
1. 使用阿里云镜像（见上文"步骤2：编译项目"）
2. 手动清理并重新下载：
   ```bash
   ./mvnw dependency:purge-local-repository
   ./mvnw clean install
   ```
3. 检查网络连接，可能需要翻墙

---

### 问题5：前端npm install失败

**解决方案**：
1. 清除缓存：
   ```bash
   npm cache clean --force
   ```
2. 删除node_modules和package-lock.json：
   ```bash
   rm -rf node_modules package-lock.json  # Linux/Mac
   rmdir /s /q node_modules              # Windows
   del package-lock.json
   ```
3. 重新安装：
   ```bash
   npm install --registry=https://registry.npmmirror.com
   ```
4. 如果仍然失败，尝试使用yarn：
   ```bash
   npm install -g yarn
   yarn install
   ```

---

### 问题6：支付宝支付无法使用

**说明**：支付宝沙箱配置在 `application-dev.yml` 中，但需要：
1. 注册支付宝开放平台账号
2. 创建应用并获取AppId
3. 配置密钥（RSA2）
4. 修改配置文件中的密钥信息

**测试时可以跳过支付功能**，直接在数据库中修改订单状态。

---

### 问题7：邮件发送失败

**错误信息**：
```
AuthenticationFailedException: Authentication failed
```

**解决方案**：
1. 确认QQ邮箱已开启SMTP服务
2. 使用授权码而非登录密码
3. 检查配置文件中的邮件配置

**暂时不需要邮件功能可以不配置**，不影响其他功能使用。

---

### 问题8：端口冲突

**错误信息**：
```
Web server failed to start. Port 8080 was already in use.
```

**解决方案**：

**方式1：关闭占用端口的进程**
```bash
# Windows
netstat -ano | findstr :8080
taskkill /F /PID <进程ID>

# Linux/Mac
lsof -i :8080
kill -9 <进程ID>
```

**方式2：修改端口**

修改 `application.yml`：
```yaml
server:
  port: 8081  # 改为其他端口
```

修改前端API地址（如果需要）

---

## 开发建议

### 1. 使用IDE开发（推荐）

**IntelliJ IDEA**：
- 安装Lombok插件
- 安装MyBatis插件
- 配置Maven
- 代码自动补全和跳转

**VS Code**：
- 安装Volar（Vue 3支持）
- 安装ESLint和Prettier
- 安装REST Client（API测试）

### 2. 热重载

**后端热重载**：
```bash
# 使用Spring Boot DevTools（已配置）
# 修改Java代码后自动重启
```

**前端热重载**：
```bash
# Vite自带热重载
# 修改Vue文件后自动刷新浏览器
```

### 3. 调试技巧

**后端调试**：
```java
// 在代码中添加断点
// IDEA中点击行号旁边设置断点
// Debug模式启动应用
```

**前端调试**：
```javascript
// 使用console.log()
console.log('用户数据:', userData);

// 使用浏览器开发者工具（F12）
// Network：查看API请求
// Console：查看日志
// Application：查看LocalStorage
```

### 4. Git版本控制

```bash
# 查看状态
git status

# 提交代码
git add .
git commit -m "完成购物车功能"

# 推送到远程
git push origin main
```

---

## 停止服务

### 停止后端
```bash
# 在运行后端的终端按 Ctrl+C
```

### 停止前端
```bash
# 在运行前端的终端按 Ctrl+C
```

### 停止Docker容器
```bash
# 停止MySQL和Redis
docker-compose down

# 或者只停止特定服务
docker-compose stop mysql
docker-compose stop redis
```

---

## 下一步

1. ✅ 后端和前端都启动成功后
2. 访问 http://localhost:5173（用户端）
3. 注册一个测试账号
4. 浏览商品、加入购物车、下单等流程
5. 访问 http://localhost:5174（管理后台）
6. 查看销售统计、管理商品等

**祝你开发顺利！** 🚀

---

**需要帮助？**
- 查看项目文档：`README.md`、`CLAUDE.md`
- 查看API文档：http://localhost:8080/swagger-ui.html
- 提交Issue：GitHub Issues
