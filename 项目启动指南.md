# 🚀 Emart 电商项目启动指南

本文档详细介绍如何使用终端启动 Emart 电商平台（用户端 + 管理员端）

---

## 📋 目录

- [前置条件](#前置条件)
- [项目结构](#项目结构)
- [启动步骤](#启动步骤)
- [验证启动](#验证启动)
- [常见问题](#常见问题)
- [停止服务](#停止服务)

---

## 🔧 前置条件

### 1. 必需软件

| 软件 | 版本要求 | 检查命令 |
|-----|---------|---------|
| **JDK** | 17+ | `java -version` |
| **Maven** | 3.6+ | `mvn -version` |
| **Node.js** | 16+ | `node -v` |
| **npm** | 8+ | `npm -v` |
| **MySQL** | 5.7+ / 8.0+ | `mysql --version` |
| **Git** | 最新版 | `git --version` |

### 2. 安装检查

在终端执行以下命令检查环境：

```bash
# 检查 JDK
java -version
# 输出示例：java version "17.0.1"

# 检查 Maven
mvn -version
# 输出示例：Apache Maven 3.8.6

# 检查 Node.js
node -v
# 输出示例：v18.17.0

# 检查 npm
npm -v
# 输出示例：9.6.7

# 检查 MySQL
mysql --version
# 输出示例：mysql  Ver 8.0.33

# 检查 Git
git --version
# 输出示例：git version 2.40.0
```

---

## 📁 项目结构

```
D:\test\project\emart\
├── emart-api/                    # 后端项目（Spring Boot）
│   ├── src/
│   ├── target/
│   ├── pom.xml
│   └── mvnw                      # Maven Wrapper（Windows: mvnw.cmd）
│
├── emart-web/                    # 用户前端（Vue 3）
│   ├── src/
│   ├── node_modules/
│   ├── package.json
│   └── vite.config.js
│
├── emart-admin/                  # 管理员前端（Vue 3）
│   ├── src/
│   ├── node_modules/
│   ├── package.json
│   └── vite.config.js
│
└── 项目启动指南.md                # 本文档
```

---

## 🚀 启动步骤

### 重要说明：
**需要同时启动 3 个服务，请打开 3 个独立的终端窗口！**

---

## 📦 终端1：启动后端服务（Spring Boot）

### 1.1 打开第一个终端

**Windows:**
- 按 `Win + R`，输入 `cmd`，回车
- 或按 `Win + X`，选择"终端"

**Mac/Linux:**
- 按 `Cmd + Space`（Mac）或 `Ctrl + Alt + T`（Linux）
- 输入 `Terminal`，回车

### 1.2 进入后端项目目录

```bash
# Windows
cd D:\test\project\emart

# Mac/Linux
cd /test/project/emart
```

### 1.3 启动后端服务

```bash
# 使用 Maven Wrapper 启动（推荐）
./mvnw spring-boot:run

# 或者（如果没有 mvnw）
mvn spring-boot:run
```

**Windows 用户注意：**
- 如果 `./mvnw` 不工作，使用 `./mvnw.cmd`
- 或直接使用：`mvnw.cmd spring-boot:run`

### 1.4 等待启动完成

**启动成功的标志：**
```
Started EmartApplication in 8.234 seconds
Tomcat started on port(s): 8080 (http)
```

**后端信息：**
- ✅ 端口：`8080`
- ✅ API地址：http://localhost:8080
- ✅ Swagger文档：http://localhost:8080/swagger-ui.html

**⚠️ 不要关闭这个终端窗口！**

---

## 🌐 终端2：启动用户前端（Vue 3）

### 2.1 打开第二个终端

打开一个新的终端窗口（保持第一个终端运行）

### 2.2 进入用户前端目录

```bash
# Windows
cd D:\test\project\emart\emart-web

# Mac/Linux
cd /test/project/emart/emart-web
```

### 2.3 检查依赖是否安装

```bash
# 检查是否有 node_modules 目录
dir node_modules        # Windows
ls node_modules         # Mac/Linux
```

**如果没有 node_modules，先安装依赖：**
```bash
npm install
```

### 2.4 启动用户前端开发服务器

```bash
npm run dev
```

### 2.5 等待启动完成

**启动成功的标志：**
```
VITE v7.3.0  ready in 468 ms

➜  Local:   http://localhost:5174/
➜  Network: use --host to expose
```

**用户前端信息：**
- ✅ 端口：`5174`
- ✅ 访问地址：http://localhost:5174
- ✅ 自动热更新（代码修改自动刷新）

**⚠️ 不要关闭这个终端窗口！**

---

## 🔐 终端3：启动管理员前端（Vue 3）

### 3.1 打开第三个终端

再打开一个新的终端窗口（保持前两个终端运行）

### 3.2 进入管理员前端目录

```bash
# Windows
cd D:\test\project\emart\emart-admin

# Mac/Linux
cd /test/project/emart/emart-admin
```

### 3.3 检查依赖是否安装

```bash
# 检查是否有 node_modules 目录
dir node_modules        # Windows
ls node_modules         # Mac/Linux
```

**如果没有 node_modules，先安装依赖：**
```bash
npm install
```

### 3.4 启动管理员前端开发服务器

```bash
npm run dev
```

### 3.5 等待启动完成

**启动成功的标志：**
```
VITE v7.3.0  ready in 468 ms

➜  Local:   http://localhost:5175/
➜  Network: use --host to expose
```

**管理员前端信息：**
- ✅ 端口：`5175`
- ✅ 访问地址：http://localhost:5175
- ✅ 自动热更新（代码修改自动刷新）

**⚠️ 不要关闭这个终端窗口！**

---

## ✅ 验证启动

### 检查所有服务是否运行

打开浏览器，依次访问以下地址：

| 服务 | 地址 | 状态 |
|-----|------|------|
| **用户前端** | http://localhost:5174 | 应显示 Emart 首页 |
| **管理员前端** | http://localhost:5175 | 应显示管理员登录页 |
| **后端 API** | http://localhost:8080 | 应显示 404 或错误页（正常）|
| **Swagger 文档** | http://localhost:8080/swagger-ui.html | 应显示 API 文档 |

### 使用终端命令检查端口占用

**Windows:**
```bash
# 检查 8080 端口（后端）
netstat -ano | findstr :8080

# 检查 5174 端口（用户前端）
netstat -ano | findstr :5174

# 检查 5175 端口（管理员前端）
netstat -ano | findstr :5175
```

**Mac/Linux:**
```bash
# 检查端口占用
lsof -i :8080
lsof -i :5174
lsof -i :5175
```

**正确输出示例：**
```
TCP    0.0.0.0:8080           0.0.0.0:0              LISTENING       12345
TCP    0.0.0.0:5174           0.0.0.0:0              LISTENING       12346
TCP    0.0.0.0:5175           0.0.0.0:0              LISTENING       12347
```

---

## 🎯 访问系统

### 用户系统

**地址：** http://localhost:5174

**功能：**
- 浏览商品
- 搜索商品
- 加入购物车
- 提交订单
- 查看订单
- 个人中心

### 管理员系统

**地址：** http://localhost:5175

**功能：**
- 商品管理
- 订单管理
- 用户管理
- 分类管理
- 数据统计

---

## ❓ 常见问题

### 1. 端口被占用

**问题：**
```
Error: Port 8080 is already in use
```

**解决方案：**

**方案1：停止占用端口的进程**
```bash
# Windows - 查找并停止进程
netstat -ano | findstr :8080
taskkill /F /PID <进程ID>

# Mac/Linux
lsof -i :8080
kill -9 <进程ID>
```

**方案2：修改端口配置**

修改后端配置文件 `src/main/resources/application.yml`：
```yaml
server:
  port: 8081  # 改为其他端口
```

---

### 2. Maven 依赖下载失败

**问题：**
```
Failed to download dependencies
```

**解决方案：**
```bash
# 清理并重新下载依赖
./mvnw clean install -U

# 或者配置国内镜像源
```

---

### 3. npm install 失败

**问题：**
```
npm ERR! code ENOSELF
```

**解决方案：**
```bash
# 清理缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json  # Mac/Linux
rmdir /s /q node_modules                # Windows

# 重新安装
npm install
```

---

### 4. 数据库连接失败

**问题：**
```
Communications link failure
```

**解决方案：**

1. 检查 MySQL 是否启动：
```bash
# Windows
net start | findstr MySQL

# Mac/Linux
ps aux | grep mysql
```

2. 检查数据库配置：
```bash
# 登录 MySQL
mysql -uroot -p

# 查看数据库
SHOW DATABASES;
# 应该看到 emart 数据库
```

3. 修改数据库配置：
编辑 `src/main/resources/application-dev.yml`：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/emart?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 你的密码
```

---

### 5. 后端启动超时

**问题：**
```
Timeout waiting for Tomcat to start
```

**解决方案：**
```bash
# 增加启动超时时间
export MAVEN_OPTS="-Xmx1024m -XX:MaxPermSize=512m"

# 或者跳过测试
./mvnw spring-boot:run -DskipTests
```

---

### 6. 前端白屏或报错

**问题：**
浏览器打开后显示白屏

**解决方案：**

1. 检查浏览器控制台错误（F12）
2. 清理浏览器缓存
3. 重新启动前端服务：
```bash
# 按 Ctrl + C 停止服务
# 重新启动
npm run dev
```

---

### 7. CORS 跨域问题

**问题：**
```
Access to XMLHttpRequest blocked by CORS policy
```

**解决方案：**

检查后端 CORS 配置，确保允许跨域访问。

---

## 🛑 停止服务

### 停止所有服务

**方式1：逐个停止（推荐）**

在每个终端窗口中：
- 按 `Ctrl + C`
- 等待服务停止
- 关闭终端窗口

**方式2：强制关闭（Windows）**
```bash
# 查找 Java 进程
tasklist | findstr java

# 强制结束
taskkill /F /IM java.exe

# 查找 Node 进程
tasklist | findstr node

# 强制结束
taskkill /F /IM node.exe
```

**方式3：强制关闭（Mac/Linux）**
```bash
# 查找并结束进程
pkill -f java
pkill -f node
```

---

## 📊 启动总结

### 成功启动后的终端状态

| 终端 | 目录 | 命令 | 端口 | 状态 |
|-----|------|------|------|------|
| **终端1** | `emart` | `./mvnw spring-boot:run` | 8080 | ✅ 运行中 |
| **终端2** | `emart-web` | `npm run dev` | 5174 | ✅ 运行中 |
| **终端3** | `emart-admin` | `npm run dev` | 5175 | ✅ 运行中 |

### 访问地址

```
用户系统:      http://localhost:5174
管理员系统:    http://localhost:5175
后端API:       http://localhost:8080
Swagger文档:   http://localhost:8080/swagger-ui.html
```

---

## 💡 开发建议

### 1. 终端工具推荐

**Windows:**
- Windows Terminal（推荐）
- PowerShell
- Git Bash

**Mac:**
- Terminal（自带）
- iTerm2（推荐）

**Linux:**
- GNOME Terminal
- Konsole

### 2. 多终端管理

**推荐方案：**
- 使用终端标签页（`Ctrl + Shift + T`）
- 或使用分屏功能（`Ctrl + Shift + D`）
- 这样可以在一个窗口管理多个终端

### 3. 日志查看

**后端日志：**
```
终端1会实时显示后端日志
包括：API请求、数据库操作、错误信息等
```

**前端日志：**
```
终端2和终端3显示前端构建日志
浏览器控制台（F12）显示运行时日志
```

### 4. 热更新

**前端热更新：**
- 修改 `.vue` 文件会自动刷新浏览器
- 修改 `.js` 文件会自动刷新浏览器
- 无需手动重启

**后端热更新：**
- Spring Boot DevTools 自动重载
- 修改 Java 文件后自动编译
- 部分修改需要重启（如配置文件）

---

## 🎓 下一步

启动成功后，可以：

1. **浏览用户系统** - 查看商品、注册登录
2. **测试购物流程** - 加入购物车、下单、支付
3. **访问管理员系统** - 管理商品、订单
4. **查看API文档** - Swagger UI 测试接口
5. **开始开发** - 修改代码、添加功能

---

## 📞 获取帮助

遇到问题？

1. 查看本文档的[常见问题](#常见问题)章节
2. 检查终端输出的错误信息
3. 查看浏览器控制台（F12）的错误
4. 检查后端日志（终端1）

---

## 🎉 总结

**快速启动三步曲：**

```bash
# 终端1：后端
cd D:\test\project\emart
./mvnw spring-boot:run

# 终端2：用户前端
cd D:\test\project\emart\emart-web
npm run dev

# 终端3：管理员前端
cd D:\test\project\emart\emart-admin
npm run dev
```

**访问：**
- 用户：http://localhost:5174
- 管理员：http://localhost:5175

**祝开发愉快！** 🚀
